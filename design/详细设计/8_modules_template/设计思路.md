# 模板模块设计

## 一、设计目标

模板模块负责：
1. **模板管理**：角色、规则、场景模板的创建和管理
2. **模板渲染**：将变量替换为实际值
3. **模板继承**：支持模板继承和覆盖

## 二、目录结构

```
8_modules_template/
├── 设计思路.md                              # ✅ 本文件
├── schemas.md                               # 数据结构
├── service.md                               # 服务层（合并后的统一定义）
├── renderer.md                              # 模板渲染器
└── repository.md                            # 数据访问层
```

## 三、接口引用说明

> ⚠️ **重要**: `ITemplateService` 和 `ITemplateRenderer` 接口已在 `service.md` 中统一定义。
> 本文件仅保留核心设计思路，详细接口定义请参考 `service.md`。

```python
# 引用方式（使用 service.md 中定义的接口）
from .service import ITemplateService, ITemplateRenderer
```

### 3.1 核心功能说明

| 功能 | 说明 | 对应接口 |
|------|------|----------|
| 模板CRUD | 创建、读取、更新、删除模板 | `ITemplateService` |
| 模板渲染 | 将变量替换为实际值 | `ITemplateService.render()` |
| 变量提取 | 从模板内容中提取变量 | `ITemplateRenderer.extract_variables()` |
| 语法验证 | 验证模板语法 | `ITemplateRenderer.validate_syntax()` |
| 模板搜索 | 按名称、类型搜索模板 | `ITemplateService.search()` |
| 模板复制 | 复制现有模板 | `ITemplateService.duplicate()` |

## 四、模板渲染器实现

```python
import re
from typing import Dict, Any

class TemplateRenderer:
    """模板渲染器"""
    
    VARIABLE_PATTERN = re.compile(r'\{\{(\w+)\}\}')
    
    async def render(
        self,
        template: Template,
        variables: Dict[str, Any]
    ) -> Dict[str, Any]:
        """渲染模板"""
        result = {}
        
        for key, value in template.content.items():
            if isinstance(value, str):
                result[key] = await self.render_string(value, variables)
            elif isinstance(value, dict):
                result[key] = await self._render_dict(value, variables)
            elif isinstance(value, list):
                result[key] = await self._render_list(value, variables)
            else:
                result[key] = value
        
        return result
    
    async def render_string(
        self,
        text: str,
        variables: Dict[str, Any]
    ) -> str:
        """渲染字符串模板"""
        def replace_var(match):
            var_name = match.group(1)
            return str(variables.get(var_name, match.group(0)))
        
        return self.VARIABLE_PATTERN.sub(replace_var, text)
    
    async def _render_dict(
        self,
        obj: Dict[str, Any],
        variables: Dict[str, Any]
    ) -> Dict[str, Any]:
        """渲染字典"""
        result = {}
        for key, value in obj.items():
            if isinstance(value, str):
                result[key] = await self.render_string(value, variables)
            elif isinstance(value, dict):
                result[key] = await self._render_dict(value, variables)
            else:
                result[key] = value
        return result
    
    async def _render_list(
        self,
        items: List[Any],
        variables: Dict[str, Any]
    ) -> List[Any]:
        """渲染列表"""
        result = []
        for item in items:
            if isinstance(item, str):
                result.append(await self.render_string(item, variables))
            elif isinstance(item, dict):
                result.append(await self._render_dict(item, variables))
            else:
                result.append(item)
        return result
```
